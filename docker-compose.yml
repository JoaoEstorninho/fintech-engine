version: "3.9"

services:
  api:
    build: .
    container_name: fintech-api
    ports:
      - "8000:8000"
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis

  redis:
    image: redis:7
    container_name: fintech-redis
    ports:
      - "6379:6379"